export const headerWrapperStyles = (color: string) => {
    return {
      color: color === 'dark' ? '#fff' : '#33404F',
      backgroundColor: color === 'dark' ? '#000' : '#fff',
    };
};

export const headerStyles = (color: string) => {
    return {
      color: color === 'dark' ? '#fff' : '#33404F',
    };
};